// Troque este IP quando o IPv4 do seu PC mudar.
// No Windows, use: ipconfig
// O backend precisa estar rodando com: uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

export const API_URL = "http://192.168.68.110:8000";
